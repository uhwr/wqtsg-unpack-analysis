# wx3b9352e6b254ed2b_unpacked 审计补充报告

## 1. 本轮新增产物

已将 `app-service.js` 中的 `define(...)` 模块拆分到桌面目录：

```text
C:\Users\Administrator\Desktop\wx3b9352e6b254ed2b_unpacked_还原模块
```

共提取 36 个模块，并生成：

```text
_manifest.json
```

主要还原文件：

```text
config.js
app.js
service/dataService.js
service/userService.js
utils/http.js
utils/util.js
utils/schoolChange.js
utils/watch.js
utils/wxcharts.js
components/dialog/dialog.js
components/editor/editor.js
components/layout/layout.js
components/select/select.js
pages/login/login.js
pages/index/index.js
pages/tongji/tongji.js
pages/free/free.js
pages/question/question.js
pages/qrcode/qrcode.js
pages/black/black.js
pages/blue/blue.js
pages/blue/auth.js
pages/message/message.js
```

注意：PowerShell 输出中中文目录名可能显示成乱码，这是终端编码问题；文件系统实际路径按 Unicode 创建。

## 2. 组件补充分析

### 2.1 `components/dialog/dialog.js`

用途：通用弹窗。

属性：

```text
title
content
cancelText
confirmText
maskClose
disabledBtn
theme
maskClass
```

方法：

```text
showDialog()
hideDialog()
clickMask()
_cancelEvent()
_confirmEvent()
```

页面通过 `selectComponent("#dialog")` 等方式调用弹窗。

### 2.2 `components/select/select.js`

用途：下拉选择器。

属性：

```text
options
text
checked
width
height
backgroud
```

方法：

```text
ready()
select()
toggle()
show()
hide()
```

用于馆区选择、日期范围选择、学校选择等场景。

### 2.3 `components/layout/layout.js`

用途：座位布局展示与交互。

属性：

```text
width
height
propLayout
```

核心数据：

```text
Layout
maxHeight
maxWidth
scale
x
y
cx
cy
colors
show
```

方法：

```text
setLayout()
setColors()
seatClick()
reload()
bindTouchStart()
bindTouchMove()
bindTouchEnd()
```

该组件支持：

```text
座位网格渲染
座位点击事件
双指缩放 / 移动
不同座位状态颜色
```

### 2.4 `components/editor/editor.js`

用途：通知发送页的富文本编辑器。

核心方法：

```text
onEditorReady()
onStatusChange()
readOnlyChange()
format()
insertLink()
syncHtml()
decodeHtml()
```

它会通过 `syncHtml()` 同步编辑器 HTML 内容给父页面，父页面用于 `sendMsg` 通知内容。

## 3. 逐页详细逻辑

### 3.1 登录页 `pages/login/login.js`

数据字段：

```text
name
passwd
projectName
userInfo
logo
canIuse
ready
```

方法：

```text
bindKeyInput()
loginSuccess()
login()
onLoad()
```

接口调用：

```text
userService.login(name, passwd)
```

微信 API：

```text
wx.setStorageSync
wx.redirectTo
```

关键流程：

```text
用户输入账号密码
  ↓
login()
  ↓
userService.login
  ↓
loginSuccess()
  ↓
accessToken 改写为 adm_account|accessToken
  ↓
写入 storage: userInfo
  ↓
redirectTo ../index/index
```

审计重点：

```text
登录失败提示是否泄露账号状态
是否存在过弱密码策略，需服务端确认
accessToken 生命周期和吊销能力
```

### 3.2 首页 `pages/index/index.js`

数据字段：

```text
indexMenu
yesterday.reserve_sum
yesterday.mark_sum
yesterday.hold_sum
yesterday.cancel_sum
yesterday.confirm_sum
version
```

菜单入口：

```text
座位监督管理 -> free
在线二维码 -> qrcode
问题回复 -> question
黑名单 -> black
通知发送 -> message
```

方法：

```text
tongjiClick()
onLoad()
schChangeInPage()
```

接口调用：

```text
dataService.getCurrentSch
dataService.index
dataService.setCurrentSch
```

关键流程：

```text
checkRedirect 确认登录态
  ↓
读取 currentSch
  ↓
调用 index(accessToken, sch_id)
  ↓
写入 schList / currentSch / yesterday statis
```

审计重点：

```text
首页返回的 schList 是否只包含管理员授权学校
切换 currentSch 后服务端是否仍做权限校验
```

### 3.3 统计页 `pages/tongji/tongji.js`

数据字段：

```text
libs
dates
libSelect
dateSelect
tabActive
canvasShow
tableData
```

方法：

```text
schClick()
selectShowHandler()
libSelectHandler()
dateSelectHandler()
tabClick()
schChangeInPage()
fetchData()
displayData()
fetchApiData()
rewriteChart()
touchHandler()
moveHandler()
touchEndHandler()
```

接口调用：

```text
dataService.getCurrentSch
dataService.tongji
```

图表依赖：

```text
utils/wxcharts.js
```

审计重点：

```text
sch_id / lib_id 是否可越权枚举
统计数据是否涉及敏感运营数据
不同管理员是否有统计字段访问差异
```

### 3.4 座位管理页 `pages/free/free.js`

数据字段：

```text
layoutWidth
layoutHeight
libSelect
libs
layout
layoutBar
proofs
choosedSeat
choosedSeatLength
choosedSeatSelected
num
talk
proofDialogTitle
showUserInfo
userinfo
userinfoY
```

方法较多，核心分组如下。

座位布局：

```text
fetchApiData()
libSelectHandler()
pageChangeHandler()
schChangeInPage()
seatClickHandler()
refrashClickHandler()
```

座位选择：

```text
choosedSeat()
chooseNumHandler()
chooseSeat()
chooseNum()
findSeat()
getSeatsKeyArray()
addNumHandler()
delNumHandler()
```

凭证图片：

```text
addProof()
removeProof()
urlTobase64()
hideProofDialog()
```

批量操作：

```text
batMarkHandler()
batMarkSubmit()
batFreeHandler()
```

接口调用：

```text
dataService.getCurrentSch
dataService.free
dataService.seatUserInfo
dataService.batMark
dataService.batFree
dataService.uploadProof
dataService.removeProof
```

微信 API：

```text
wx.showModal
wx.chooseImage
wx.getFileSystemManager
```

审计重点：

```text
seat_keys 是否可被构造为跨馆区/跨学校座位
batFree 是否检查座位当前状态
batMark 是否检查 proofId 属于当前操作者/学校
uploadProof 是否限制大小、格式和频率
seatUserInfo 是否暴露学生个人信息，需做权限和审计
```

### 3.5 问题回复页 `pages/question/question.js`

数据字段：

```text
tabActive
qstList
page
pageTotal
qst
answermsg
```

方法：

```text
tabClick()
answerHanlder()
answerCancleHandler()
formSubmitHandler()
getRemovedLIst()
schChangeInPage()
onReady()
onReachBottom()
fetchData()
fetchApiData()
refrashClickHandler()
onLoad()
```

接口调用：

```text
dataService.getCurrentSch
dataService.qst
dataService.answer
```

审计重点：

```text
qst_id 是否可越权回复其他学校问题
answer_msg 是否做长度、内容安全和 HTML 过滤
分页接口是否可枚举其他学校问题
```

### 3.6 二维码页 `pages/qrcode/qrcode.js`

数据字段：

```text
url
timeList
ttl
expireAt
expire
chooseTime
interval
```

方法：

```text
schChangeInPage()
downloadHandler()
timeChangeHandler()
reflushHandler()
timeSelectHanlder()
cancelTimeHanlder()
cancelImgHanlder()
confirmTimeHanlder()
fetchData()
fetchApiData()
```

接口调用：

```text
dataService.getCurrentSch
dataService.qrcode
dataService.setQrCodeExpire
```

微信 API：

```text
wx.showModal
```

审计重点：

```text
isRefresh 是否允许高频刷新
ttl 是否可设置过小/过大
二维码 URL 是否可被未授权访问
setQrCodeExpire 是否按学校权限校验
```

### 3.7 黑名单页 `pages/black/black.js`

数据字段：

```text
tabActive
list
searchList
page
pageTotal
searchText
searchType
searchValue
```

搜索类型：

```text
user_mobile
user_student_no
```

方法：

```text
bindKeyInput()
searchTypeClick()
searchTypeClose()
searchTypeChange()
schChangeInPage()
searchBlur()
removeBlackHanlder()
onReady()
onReachBottom()
fetchData()
fetchApiData()
onLoad()
```

接口调用：

```text
dataService.getCurrentSch
dataService.black
dataService.removeBlack
```

微信 API：

```text
wx.showModal
```

审计重点：

```text
removeBlack 仅传 userMobile，必须服务端结合学校/管理员权限校验
searchValue 空值是否返回全量数据
分页是否可大量枚举学生手机号、学号、姓名
```

### 3.8 通知发送页 `pages/message/message.js`

数据字段：

```text
endTime
title
content
userMobile
schId
showType
schList
```

方法：

```text
onLoad()
send()
bindValue()
schAll()
syncCtx()
```

接口调用：

```text
dataService.getCurrentSch
dataService.index
dataService.setCurrentSch
dataService.sendMsg
```

微信 API：

```text
wx.redirectTo
```

审计重点：

```text
schAll 是否允许选择全部学校或全部授权学校
userMobile 为空时的服务端语义
content 富文本是否可能导致 XSS 或富文本注入
sendMsg 是否做频率、范围、审计和撤回机制
```

### 3.9 签到页 `pages/blue/blue.js`

数据字段：

```text
isDebug
isAuth
hasSign
signResult
uuids
tip
url
msg
t
time
version
location
foundBeacons
hideLoadingTimer
```

方法：

```text
onLoad()
checkEnv()
tapAuth()
tapSignIn()
verifSch()
getDeviceInfo()
getLocaton()
getBeacon()
signIn()
signInNext()
stopSearch()
loadingStart()
loadingEnd()
onShareAppMessage()
alert()
getAverage()
fiterBeacons()
base64Encode()
```

微信 API：

```text
wx.request
wx.showModal
wx.showToast
wx.navigateTo
wx.openBluetoothAdapter
wx.getLocation
wx.startBeaconDiscovery
wx.getBeacons
wx.stopBeaconDiscovery
```

接口路径：

```text
POST https://wechat.v2.traceint.com/index.php/wxApp/devices.html
GET  https://wechat.v2.traceint.com/index.php/wxApp/getTime.html
POST https://wechat.v2.traceint.com/index.php/wxApp/sign.html
```

提交签到数据：

```text
t
base64(JSON.stringify(devices))
base64(JSON.stringify(location))
RSA(serverTime)
```

审计重点：

```text
devices/location 是客户端可控数据，服务端不能盲信
getTime + RSA pass 是否有时间窗口和重放保护
Beacon UUID 是否严格绑定学校和场景
定位是否做范围校验和异常检测
```

### 3.10 签到授权页 `pages/blue/auth.js`

逻辑很薄，主要加载 WebView 地址：

```text
https://wechat.v2.traceint.com/index.php/wxApp/index.html#wechat_redirect
```

方法：

```text
onHide()
onUnload()
onShareAppMessage()
```

审计重点：

```text
授权页返回参数 t 的生成、绑定和有效期
授权 URL 是否有开放重定向或参数污染风险
```

## 4. 还原模块后的审计优先级

### P0：服务端权限边界必须验证

```text
admAuth 是否检查 token 有效性
admAuth 是否检查管理员角色
每个 resolver 是否检查学校范围
每个 resolver 是否检查馆区/座位/问题/二维码归属
```

重点接口：

```text
release
setfree
removeBlack
sendMsg
answer
setQrCodeExpire
seatUserInfo
```

### P1：批量操作和枚举风险

```text
seat_keys 批量长度限制
黑名单分页枚举
问题分页枚举
统计数据枚举
学校/馆区 ID 枚举
二维码刷新频率
通知发送频率
```

### P2：内容输入和文件上传

```text
answer_msg
sendMsg.title
sendMsg.content
uploadProof.base64code
batMark.msg
```

检查：

```text
长度限制
格式校验
内容安全
HTML/富文本过滤
图片 MIME 检测
服务端存储路径安全
```

### P3：签到防伪造

```text
Beacon 数据伪造
定位数据伪造
服务器时间 pass 重放
t 参数重放
频率限制
异常设备检测
```

## 5. 建议接口测试矩阵

以下测试仅应在授权环境执行。

### 5.1 Token 类

```text
空 accessToken
随机 accessToken
过期 accessToken
adm_account|accessToken 中 adm_account 被替换
accessToken 被截断或拼接额外字段
同一 token 跨设备/跨 IP 使用
```

预期：

```text
全部未授权请求返回统一错误，不泄露内部细节。
```

### 5.2 学校越权

```text
管理员 A 正常登录
修改 sch_id 为管理员 B 的学校
调用 index/tongji/free/qst/black/qrcode/sendMsg
```

预期：

```text
服务端拒绝，不返回非授权学校数据。
```

### 5.3 馆区越权

```text
sch_id 使用授权学校
lib_id 使用非授权学校的馆区
调用 free/tongji/batFree/batMark/seatUserInfo
```

预期：

```text
服务端拒绝 lib_id 与权限范围不匹配的请求。
```

### 5.4 座位批量操作

```text
seat_keys 为空
seat_keys 包含不存在座位
seat_keys 包含其他馆区座位
seat_keys 数量极大
seat_keys 重复
seat_keys 混合合法和非法值
```

预期：

```text
服务端完整校验每个 seat_key，并限制批量规模。
```

### 5.5 黑名单

```text
searchValue 为空
searchValue 模糊枚举
removeBlack 使用其他学校手机号
removeBlack 使用不存在手机号
```

预期：

```text
列表和移除均按学校/管理员权限隔离。
```

### 5.6 通知发送

```text
schId 为空
schId 为非授权学校
userMobile 为空
userMobile 为其他学校用户
content 超长
content 包含 HTML/script/外链
高频发送
```

预期：

```text
服务端限制发送范围、频率和内容，记录审计日志。
```

### 5.7 签到接口

```text
devices 为空
devices 伪造 UUID
devices 使用其他学校 Beacon
location 为空
location 远离学校
pass 重放
pass 使用过期 serverTime
t 参数重放
```

预期：

```text
服务端做时间窗口、授权参数、Beacon 归属、定位范围和频率校验。
```

## 6. 进一步工作建议

如果继续深入，建议下一步做两件事：

1. 对还原模块进行格式化/反混淆，尤其是：

```text
service/dataService.js
pages/free/free.js
pages/blue/blue.js
components/layout/layout.js
```

2. 建立接口字典表，字段包括：

```text
页面
方法
GraphQL 操作
变量
是否读操作
是否写操作
敏感等级
权限校验点
测试用例编号
```

## 7. 本轮结论

本轮已将打包代码拆分为独立模块，并按页面完成了数据字段、方法、接口调用和风险点映射。当前最值得继续投入的方向是：服务端授权边界验证、座位批量操作、通知发送、黑名单移除、签到防伪造。客户端代码本身主要提供接口与业务流信息，真正安全性取决于服务端对这些操作的强制校验。
